export const appName = 'enso-crm docs';
// Docs are mounted at the app root (baseUrl '/') and served under /docs via
// Next basePath, so internal routes resolve to /docs/* at runtime.
export const docsRoute = '/';
export const docsImageRoute = '/og/docs';
export const docsContentRoute = '/llms.mdx/docs';

export const gitConfig = {
  user: 'corporateready',
  repo: 'enso-crm',
  branch: 'main',
};
